// Test removed: previously caused parser errors in this toolchain.
// If you want to reintroduce a compile/runtime test for CoinBalance,
// I can create a version that mirrors the stdlib test patterns.
